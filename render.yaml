services:
  - type: worker
    name: net-joker-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python net_joker_bot.py
    envVars:
      - key: TOKEN
        sync: false
